name: CI
on:
    - push
jobs:
    backend: 
      name: Build
      runs-on: ubuntu-latest
      env:
        BRANCHNAME: default55555
      steps:
        - name: Filter Github branch
          run: |
            BRANCHNAME=${GITHUB_REF#refs/heads/}
            echo "Filter Branch name is $BRANCHNAME"
            echo "GITHUB_FILTER_BRANCH_NAME=$FILTER_BRANCH_NAME" >> $GITHUB_ENV

        - name: Filter Github branch2
          run: |
            echo $GITHUB_FILTER_BRANCH_NAME
            size=${#GITHUB_FILTER_BRANCH_NAME} 
            echo "$size"


          
